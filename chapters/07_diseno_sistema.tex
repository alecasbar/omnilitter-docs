% ============================================================
%  Capítulo -- Diseño de la solución
% ============================================================

\chapter{Diseño de la solución}

Este capítulo describe las decisiones de diseño adoptadas para materializar los
requisitos funcionales y no funcionales de OmniLitter en una arquitectura concreta.
Se abordan, en orden de abstracción descendente, la arquitectura general del sistema,
el diseño de la base de datos, el contrato de la API REST, la estructura de componentes
del portal web y de la aplicación móvil, y el mecanismo de sincronización \textit{offline}.

% ------------------------------------------------------------
\section{Arquitectura general}
% ------------------------------------------------------------

OmniLitter sigue una arquitectura de \textbf{tres capas} clásica adaptada a un
contexto multiplataforma con requisito de operación sin conexión:

\begin{enumerate}
  \item \textbf{Capa de presentación.} Comprende dos clientes independientes: el portal web,
    orientado a la consulta y gestión de datos por parte de investigadores y administradores, y
    la aplicación móvil, orientada a la captura de observaciones en campo.
  \item \textbf{Capa de lógica de negocio.} Una API REST centralizada planteada con
    Node.js + Express actúa como único punto de acceso a los datos, aplica las reglas de
    negocio y gestiona la autenticación mediante \textit{tokens} JWT.
  \item \textbf{Capa de persistencia.} PostgreSQL almacena el estado canónico del sistema.
    Adicionalmente, la aplicación móvil mantiene una base de datos local SQLite
    que permite operar sin conexión y sincronizarse en diferido.
\end{enumerate}

La figura~\ref{fig:arquitectura} muestra un diagrama de la arquitectura del sistema.

\input{figures/arquitectura}

\subsection{Justificación de las decisiones arquitectónicas principales}

\textbf{API REST como único punto de acceso.}
Centralizar la lógica de negocio en la API evita duplicidades entre los dos clientes y
facilita la incorporación de nuevos consumidores en el futuro (p.ej., exportaciones
automatizadas por \textit{scripts} externos o integraciones con repositorios científicos).

\textbf{Separación entre portal web y aplicación móvil.}
Aunque comparten la misma API, los dos clientes tienen requisitos de experiencia de usuario
muy distintos: el portal prioriza la visualización analítica y la gestión, mientras que la
aplicación móvil prioriza la captura rápida en condiciones de campo adversas (luz solar, guantes,
sin conexión). Mantenerlos como proyectos independientes permite evolucionar cada uno a su
propio ritmo.

\textbf{Base de datos local en el móvil.}
El trabajo de campo se realiza habitualmente en zonas costeras, ribereñas o forestales
con cobertura de red intermitente o nula. La decisión de embeber una base de datos local en
el dispositivo es, por tanto, un requisito no negociable para la viabilidad del producto.

% ------------------------------------------------------------
\section{Diseño de la base de datos}
% ------------------------------------------------------------

\subsection{Modelo relacional}

El modelo de datos final de OmniLitter está formado por quince entidades. Para que
el diagrama resulte legible y sean más visibles las relaciones, se ha
dividido en tres bloques funcionales, cada uno con su propio diagrama
entidad-relación:

\begin{itemize}
  \item \textbf{Usuarios, grupos, campañas y catálogo:} \texttt{users},
    \texttt{groups}, \texttt{memberships},\allowbreak{}
    \texttt{group\_}\allowbreak{}\texttt{invitations},
    \texttt{campaigns}, \texttt{environments}, \texttt{typologies} y
    \texttt{typology\_clusters}. Recoge la gestión de acceso, la pertenencia a grupos
    de investigación, las invitaciones, la organización de campañas y el catálogo
    global de entornos y tipologías.
  \item \textbf{Muestreo, observaciones y taxonomía:} \texttt{sampling\_sessions},
    \texttt{observations}, \texttt{litter\_items}, \texttt{observation\_photos} y
    \texttt{taxonomy\_items}. Es el núcleo operativo del sistema, donde se registran
    las sesiones de campo, las observaciones de residuos, su desglose y clasificación
    y las fotografías asociadas.
  \item \textbf{Autenticación y seguridad:} \texttt{users}, \texttt{refresh\_tokens}
    y \texttt{password\_reset\_tokens}. Agrupa las entidades que dan soporte al inicio
    de sesión, la renovación de tokens y el restablecimiento de contraseña.
\end{itemize}

La entidad \texttt{sampling\_sessions} aparece en los dos primeros bloques porque
sirve de unión entre la organización de campañas y la captura de datos en campo. Las
figuras~\ref{fig:modelo_er_gestion},~\ref{fig:modelo_er_muestreo}
y~\ref{fig:modelo_er_seguridad} muestran cada uno de estos bloques.

\input{figures/modelo_er}

\subsection{Descripción de entidades}

La tabla~\ref{tab:entidades} describe los atributos clave de cada entidad y su
propósito en el sistema.

\begin{longtable}{p{3.0cm} p{4.4cm} p{6.6cm}}
  \caption{Descripción de entidades del modelo de datos.}
  \label{tab:entidades} \\
  \toprule
  \textbf{Entidad} & \textbf{Atributos principales} & \textbf{Descripción} \\
  \midrule
  \endfirsthead
  \toprule
  \textbf{Entidad} & \textbf{Atributos principales} & \textbf{Descripción} \\
  \midrule
  \endhead
  \bottomrule
  \endfoot

  \texttt{users} &
    \texttt{id}, \texttt{name}, \texttt{email}, \texttt{password\_hash},
    \texttt{role}, \texttt{is\_active}, \texttt{last\_login\_at},
    \texttt{active\_group\_id (FK)} &
    Persona registrada en el sistema. La contraseña se guarda como \textit{hash} y
    nunca en texto plano. El campo \texttt{role} indica el rol global
    (\texttt{SUPERADMIN} o \texttt{USUARIO}) y \texttt{active\_group\_id} apunta al
    grupo con el que el usuario está trabajando en cada momento. \\[4pt]

  \texttt{groups} &
    \texttt{id}, \texttt{name}, \texttt{description}, \texttt{status},
    \texttt{archived\_by\_role} &
    Grupo de investigación. Un grupo puede archivarse sin borrar sus datos
    históricos, lo que conserva la trazabilidad de las campañas. El campo
    \texttt{archived\_by\_role} registra el rol que lo archivó, para condicionar quién
    puede restaurarlo. \\[4pt]

  \texttt{memberships} &
    \texttt{user\_id (FK)}, \texttt{group\_id (FK)}, \texttt{role},
    \texttt{created\_at} &
    Tabla de unión entre usuarios y grupos, con clave primaria compuesta. El campo
    \texttt{role} toma los valores \texttt{ADMIN} o \texttt{FIELD\_RESEARCHER}, de
    modo que un mismo usuario puede tener un rol distinto en cada grupo. \\[4pt]

  \texttt{group\_}\allowbreak{}\texttt{invitations} &
    \texttt{id}, \texttt{group\_id (FK)}, \texttt{email},
    \texttt{invited\_by\_id (FK)}, \texttt{status}, \texttt{responded\_at} &
    Invitación de un usuario a un grupo a partir de su correo. El campo
    \texttt{status} sigue el ciclo \texttt{PENDING}, \texttt{ACCEPTED} o
    \texttt{REJECTED}. \\[4pt]

  \texttt{campaigns} &
    \texttt{id}, \texttt{name}, \texttt{description}, \texttt{status},
    \texttt{starts\_at}, \texttt{ends\_at}, \texttt{finished\_by\_archive},
    \texttt{group\_id (FK)} &
    Proyecto de muestreo asociado a un grupo. Agrupa varias sesiones de campo bajo un
    objetivo común. El estado evoluciona entre \texttt{DRAFT}, \texttt{ACTIVE},
    \texttt{FINISHED} y \texttt{ARCHIVED}. \\[4pt]

  \texttt{typology\_clusters} &
    \texttt{id}, \texttt{code}, \texttt{name\_es}, \texttt{name\_en} &
    Agrupación que relaciona un entorno con el conjunto de tipologías compatibles. Al
    existir como entidad propia, los entornos y las tipologías se asocian mediante
    clave foránea en lugar de por texto. \\[4pt]

  \texttt{environments} &
    \texttt{id}, \texttt{code}, \texttt{name\_es}, \texttt{name\_en},
    \texttt{typology\_cluster\_id (FK)}, \texttt{is\_active} &
    Catálogo global de tipos de entorno de muestreo, como playa, ribera, bosque,
    carretera o campo de cultivo. Cada entorno cuelga de un \texttt{typology\_cluster}
    y dispone de nombre en español e inglés. \\[4pt]

  \texttt{typologies} &
    \texttt{id}, \texttt{code}, \texttt{name\_es}, \texttt{name\_en},
    \texttt{typology\_cluster\_id (FK)}, \texttt{is\_active} &
    Catálogo global de subentornos o tipologías. Comparten \texttt{typology\_cluster}
    con su entorno, lo que permite ofrecer en la app solo las tipologías coherentes
    con el entorno elegido. \\[4pt]

  \texttt{sampling\_sessions} &
    \texttt{id}, \texttt{campaign\_id (FK)}, \texttt{observer\_id (FK)},
    \texttt{environment\_id (FK)}, \texttt{typology\_id (FK)},
    \texttt{environment\_name\_snapshot}, \texttt{typology\_name\_snapshot},
    \texttt{name}, \texttt{sampling\_method}, \texttt{grid\_cell\_size\_meters},
    \texttt{transect\_width\_meters}, \texttt{duration\_minutes},
    \texttt{gps\_start\_lat/lng}, \texttt{gps\_end\_lat/lng}, \texttt{start\_time},
    \texttt{end\_time}, \texttt{status}, \texttt{taxonomy}, \texttt{track\_points},
    \texttt{sampled\_area}, \texttt{sync\_state} &
    Sesión de muestreo realizada en campo. Referencia el entorno y la tipología del
    catálogo y, al mismo tiempo, guarda su nombre en los campos \textit{snapshot}.
    Registra el método de muestreo (\texttt{LIBRE}, \texttt{CUADRICULA} o
    \texttt{TRANSECTO}), sus parámetros, la duración y las coordenadas de inicio y
    fin. Los campos \texttt{track\_points} y \texttt{sampled\_area} almacenan el
    recorrido y el área muestreada, y \texttt{sync\_state} controla el estado de
    sincronización. \\[4pt]

  \texttt{observations} &
    \texttt{id}, \texttt{session\_id (FK)}, \texttt{observer\_id (FK)},
    \texttt{taxonomy\_item\_id (FK)}, \texttt{latitude}, \texttt{longitude},
    \texttt{observed\_at}, \texttt{sync\_status}, \texttt{notes},
    \texttt{photo\_urls} &
    Registro de un residuo avistado dentro de una sesión. Conserva la posición
    (\texttt{latitude} y \texttt{longitude}), el instante de captura y la referencia
    a la taxonomía. Es la unidad básica de dato científico del sistema. \\[4pt]

  \texttt{litter\_items} &
    \texttt{id}, \texttt{observation\_id (FK)}, \texttt{category}, \texttt{quantity},
    \texttt{material}, \texttt{notes} &
    Desglose de los residuos contabilizados en una observación. El campo
    \texttt{category} usa la clasificación por material (\texttt{PLASTIC},
    \texttt{METAL}, \texttt{GLASS} y demás categorías) y \texttt{quantity} recoge la
    cantidad como entero positivo. \\[4pt]

  \texttt{observation\_photos} &
    \texttt{id}, \texttt{observation\_id (FK)}, \texttt{uploaded\_by\_id (FK)},
    \texttt{file\_path}, \texttt{file\_name}, \texttt{mime\_type}, \texttt{file\_size},
    \texttt{checksum\_sha256}, \texttt{width}, \texttt{height}, \texttt{source},
    \texttt{captured\_at}, \texttt{exif\_data}, \texttt{metadata} &
    Fotografía asociada a una observación. Almacena los datos del fichero, un
    \texttt{checksum} SHA-256 para verificar la integridad tras la sincronización y
    los metadatos EXIF de la imagen cuando están disponibles. \\[4pt]

  \texttt{taxonomy\_items} &
    \texttt{id}, \texttt{code}, \texttt{name\_es}, \texttt{name\_en},
    \texttt{category}, \texttt{subcategory}, \texttt{parent\_id (FK)},
    \texttt{version}, \texttt{is\_active} &
    Catálogo jerárquico de tipos de residuo basado en listas comunes de categorías.
    La referencia \texttt{parent\_id} hacia la propia entidad permite organizar la
    taxonomía por niveles y descargarla completa para usarla sin conexión. \\[4pt]

  \texttt{refresh\_tokens} &
    \texttt{id}, \texttt{token\_hash}, \texttt{user\_id (FK)}, \texttt{expires\_at},
    \texttt{revoked\_at} &
    Token de renovación de sesión. Se guarda únicamente su \textit{hash}, junto con la
    fecha de expiración y, en su caso, la de revocación. \\[4pt]

  \texttt{password\_reset\_tokens} &
    \texttt{id}, \texttt{token\_hash}, \texttt{user\_id (FK)}, \texttt{expires\_at},
    \texttt{used\_at} &
    Token de restablecimiento de contraseña. Es de un solo uso: el campo
    \texttt{used\_at} marca el momento en que se consume y lo invalida. \\

\end{longtable}

\subsection{Decisiones de diseño de la base de datos}

\textbf{UUID como clave primaria en registros sincronizables.}
Los registros que pueden crearse en el dispositivo móvil antes de comunicarse con el
servidor utilizan identificadores UUID generados en el propio cliente. Esto resuelve
las colisiones de claves en escenarios de creación offline: dos dispositivos que creen
una sesión o una observación sin conexión obtienen identificadores únicos que no chocan
al sincronizarse con el servidor.

\textbf{Modelo de roles en dos niveles.}
El control de acceso separa el rol global del usuario, almacenado en
\texttt{users.role} con los valores \texttt{SUPERADMIN} o \texttt{USUARIO}, del rol que
tiene dentro de cada grupo, almacenado en \texttt{memberships.role} como \texttt{ADMIN}
o \texttt{FIELD\_RESEARCHER}. El rol operativo no es una propiedad fija de la persona,
sino de su relación con un grupo concreto, por lo que un mismo investigador puede
administrar un grupo y participar como investigador de campo en otro. El superadmin
queda reservado a la administración global de la plataforma.

\textbf{Catálogo normalizado de entornos con copia histórica en la sesión.}
El catálogo de entornos y tipologías se modela con tres entidades relacionadas por
clave foránea, no por texto. La entidad \texttt{typology\_clusters} agrupa cada entorno
con las tipologías compatibles, y tanto \texttt{environments} como \texttt{typologies}
cuelgan del mismo \textit{cluster}. Así, al elegir un entorno la aplicación ofrece solo
las tipologías coherentes con él. Cada registro del catálogo guarda su nombre en español
e inglés (\texttt{name\_es} y \texttt{name\_en}), lo que permite al superadmin configurar
ambos idiomas desde el portal.

Cuando se crea una sesión, esta referencia el entorno y la tipología mediante clave
foránea real (\texttt{environment\_id} y \texttt{typology\_id}) y, al mismo tiempo, copia
su nombre en los campos \texttt{environment\_name\_snapshot} y
\texttt{typology\_name\_snapshot}. La clave foránea mantiene la integridad con el catálogo
vigente, mientras que la copia conserva el valor utilizado en el momento del muestreo
aunque el catálogo cambie después. Esta combinación evita inconsistencias en los datos
históricos y simplifica la sincronización de la app, que no necesita resolver referencias
contra un catálogo que pudo cambiar mientras el dispositivo estaba sin conexión.

\textbf{Separación entre observación y desglose de residuos.}
Una observación representa un punto avistado, con sus coordenadas e instante de
captura, mientras que \texttt{litter\_items} recoge los residuos contabilizados en ese
punto, con su categoría por material y su cantidad. Separar ambos conceptos permite
asociar varios tipos de residuo a una misma observación sin duplicar la información de
posición, y deja la clasificación detallada en manos de \texttt{taxonomy\_items}.

\textbf{Precisión geográfica con tipo decimal.}
Las coordenadas de las observaciones se almacenan con tipo \texttt{decimal} de precisión
fija (nueve dígitos, seis de ellos decimales) en lugar de coma flotante. Así se obtiene
una precisión cercana a los diez centímetros y se evitan los errores de redondeo propios
del punto flotante, algo necesario para una herramienta cuya utilidad depende de la
calidad de la georreferenciación.

\textbf{Estados de sincronización en sesiones y observaciones.}
Tanto \texttt{sampling\_sessions} como \texttt{observations} incluyen un campo de estado
de sincronización basado en el enum \texttt{SyncStatus} (\texttt{SYNCED},
\texttt{PENDING}, \texttt{FAILED} o \texttt{CONFLICT}). Este campo sostiene el flujo de
trabajo sin conexión, ya que permite distinguir qué registros están pendientes de
subir, cuáles se han confirmado en el servidor y cuáles han fallado.

\textbf{Estrategias de borrado según el tipo de relación.}
El esquema aplica reglas de borrado coherentes con el valor de cada relación. Los datos
estrictamente dependientes se eliminan en cascada junto a su contenedor: las membresías
e invitaciones de un grupo, las sesiones de una campaña y las observaciones, residuos y
fotos asociados a una sesión. En cambio, las referencias al autor
(\texttt{observer\_id} y \texttt{uploaded\_by\_id}) usan una regla restrictiva, de modo
que no se puede borrar un usuario que tenga datos de campo asociados y se preserva la
autoría. Las campañas pueden quedar sin grupo mediante \texttt{SetNull} para mantener el
histórico si se elimina el grupo, y el ítem taxonómico de una observación también se
puede poner a nulo para no perder el registro científico.

En el catálogo ambiental se combinan restricciones y copia histórica. El entorno de una
sesión usa una relación restrictiva, porque toda sesión debe conservar un contexto
ambiental válido. La tipología puede ponerse a nulo si se elimina del catálogo, pero la
sesión mantiene \texttt{typology\_name\_snapshot}; de forma equivalente,
\texttt{environment\_name\_snapshot} conserva el nombre visible del entorno utilizado en
campo. Así se evita que las tareas de mantenimiento del catálogo degraden los datos ya
registrados.

\textbf{JSON para datos de estructura variable.}
Los campos \texttt{track\_points}, \texttt{sampled\_area}, \texttt{exif\_data} y
\texttt{metadata} se almacenan como JSON. El recorrido y el área muestreada tienen una
geometría que depende del método de muestreo, y los metadatos EXIF varían según el
modelo de cámara. Guardar estos datos como JSON evita multiplicar columnas opcionales
y permite ampliar la información asociada sin migrar el esquema.

% ------------------------------------------------------------
\section{Diseño de la API REST}
% ------------------------------------------------------------

La API sigue los principios REST: interfaz uniforme, comunicación sin estado,
recursos identificados por URI y representaciones en formato JSON. Todos los
\textit{endpoints} protegidos requieren el encabezado
\texttt{Authorization: Bearer <token>}, donde el \textit{token} es un JWT firmado
emitido en el \textit{endpoint} de autenticación.

El cuadro~\ref{tab:endpoints} recoge los \textit{endpoints} principales agrupados
por módulo funcional. En lugar de presentar todas las rutas en una única tabla
continua, se separan por bloque para que resulte más visible qué parte de la API
atiende cada conjunto de operaciones.

\begin{center}
  \captionof{table}{Principales \textit{endpoints} de la API REST de OmniLitter.}
  \label{tab:endpoints}
\end{center}

\def\omniendpointheader{%
  \textbf{HTTP} & \textbf{Ruta} & \textbf{Acceso} & \textbf{Uso} \\
  \midrule
}

\begin{tcolorbox}[breakable,title=Autenticación,colback=gray!4,colframe=black!55,fonttitle=\bfseries]
\footnotesize
\begin{tabularx}{\linewidth}{@{}p{1.25cm}p{5.35cm}p{1.9cm}X@{}}
\omniendpointheader
\texttt{POST} & \path{/api/auth/login} & Público & Emite JWT de acceso y refresco \\
\texttt{POST} & \path{/api/auth/register} & Público & Registra una cuenta nueva \\
\texttt{POST} & \path{/api/auth/recover-password} & Público & Inicia la recuperación de contraseña \\
\texttt{POST} & \path{/api/auth/reset-password} & Público & Restablece la contraseña con un \textit{token} \\
\texttt{POST} & \path{/api/auth/refresh} & Público & Renueva el JWT de acceso \\
\texttt{POST} & \path{/api/auth/logout} & Usuario & Invalida el \textit{token} de refresco \\
\end{tabularx}
\end{tcolorbox}

\begin{tcolorbox}[breakable,title=Usuarios,colback=gray!4,colframe=black!55,fonttitle=\bfseries]
\footnotesize
\begin{tabularx}{\linewidth}{@{}p{1.25cm}p{5.35cm}p{1.9cm}X@{}}
\omniendpointheader
\texttt{GET} & \path{/api/users} & Admin & Lista todos los usuarios \\
\texttt{POST} & \path{/api/users} & Admin & Crea un usuario \\
\texttt{GET} & \path{/api/users/me} & Usuario & Datos del usuario autenticado \\
\texttt{GET} & \path{/api/users/me/groups} & Usuario & Grupos del usuario y grupo activo \\
\texttt{PATCH} & \path{/api/users/:id} & Admin & Actualiza datos, rol o estado \\
\end{tabularx}
\end{tcolorbox}

\begin{tcolorbox}[breakable,title=Grupos e invitaciones,colback=gray!4,colframe=black!55,fonttitle=\bfseries]
\footnotesize
\begin{tabularx}{\linewidth}{@{}p{1.25cm}p{5.35cm}p{1.9cm}X@{}}
\omniendpointheader
\texttt{GET} & \path{/api/groups} & Usuario & Lista grupos del usuario \\
\texttt{POST} & \path{/api/groups} & Admin & Crea un grupo \\
\texttt{PUT} & \path{/api/groups/:id} & Admin & Edita un grupo \\
\texttt{GET} & \path{/api/groups/:id/backup} & Admin & Descarga copia de seguridad JSON \\
\texttt{POST} & \path{/api/group-invitations/:id/accept} & Usuario & Acepta una invitación \\
\end{tabularx}
\end{tcolorbox}

\begin{tcolorbox}[breakable,title=Campañas,colback=gray!4,colframe=black!55,fonttitle=\bfseries]
\footnotesize
\begin{tabularx}{\linewidth}{@{}p{1.25cm}p{5.35cm}p{1.9cm}X@{}}
\omniendpointheader
\texttt{GET} & \path{/api/campaigns} & Usuario & Lista campañas del grupo activo \\
\texttt{POST} & \path{/api/campaigns} & Admin & Crea una campaña \\
\texttt{PUT} & \path{/api/campaigns/:id} & Admin & Edita una campaña \\
\end{tabularx}
\end{tcolorbox}

\begin{tcolorbox}[breakable,title=Catálogo científico,colback=gray!4,colframe=black!55,fonttitle=\bfseries]
\footnotesize
\begin{tabularx}{\linewidth}{@{}p{1.25cm}p{5.35cm}p{1.9cm}X@{}}
\omniendpointheader
\texttt{GET} & \path{/api/catalog/environments} & Usuario & Lista entornos del catálogo \\
\texttt{POST} & \path{/api/catalog/environments} & Superadmin & Crea un entorno \\
\texttt{GET} & \path{/api/catalog/typologies} & Usuario & Lista tipologías del catálogo \\
\end{tabularx}
\end{tcolorbox}

\begin{tcolorbox}[breakable,title=Sesiones de muestreo,colback=gray!4,colframe=black!55,fonttitle=\bfseries]
\footnotesize
\begin{tabularx}{\linewidth}{@{}p{1.25cm}p{5.35cm}p{1.9cm}X@{}}
\omniendpointheader
\texttt{GET} & \path{/api/sessions} & Usuario & Lista sesiones accesibles \\
\texttt{POST} & \path{/api/sessions} & Usuario & Crea una sesión \\
\texttt{GET} & \path{/api/sessions/:id} & Usuario & Obtiene una sesión con observaciones \\
\texttt{GET} & \path{/api/sessions/tracks} & Usuario & Recorridos y áreas muestreadas \\
\texttt{PATCH} & \path{/api/sessions/:id} & Usuario & Actualiza metadatos o estado \\
\end{tabularx}
\end{tcolorbox}

\begin{tcolorbox}[breakable,title=Observaciones y fotos,colback=gray!4,colframe=black!55,fonttitle=\bfseries]
\footnotesize
\begin{tabularx}{\linewidth}{@{}p{1.25cm}p{5.35cm}p{1.9cm}X@{}}
\omniendpointheader
\texttt{GET} & \path{/api/observations} & Usuario & Lista observaciones con filtros \\
\texttt{POST} & \path{/api/observations} & Usuario & Registra una observación \\
\texttt{GET} & \path{/api/observations/:id/photos} & Usuario & Lista fotos de una observación \\
\texttt{POST} & \path{/api/observations/:id/photos} & Usuario & Sube una foto \textit{multipart} \\
\texttt{GET} & \path{/api/observations/:id/photos/:pid/file} & Usuario & Sirve el archivo de imagen \\
\end{tabularx}
\end{tcolorbox}

\begin{tcolorbox}[breakable,title=Taxonomía,colback=gray!4,colframe=black!55,fonttitle=\bfseries]
\footnotesize
\begin{tabularx}{\linewidth}{@{}p{1.25cm}p{5.35cm}p{1.9cm}X@{}}
\omniendpointheader
\texttt{GET} & \path{/api/taxonomy/items} & Usuario & Descarga la taxonomía filtrable por versión \\
\end{tabularx}
\end{tcolorbox}

\begin{tcolorbox}[breakable,title=Métricas,colback=gray!4,colframe=black!55,fonttitle=\bfseries]
\footnotesize
\begin{tabularx}{\linewidth}{@{}p{1.25cm}p{5.35cm}p{1.9cm}X@{}}
\omniendpointheader
\texttt{GET} & \path{/api/metrics} & Usuario & Métricas agregadas para el dashboard \\
\texttt{GET} & \path{/api/metrics/observations} & Usuario & Datos por observación para analítica \\
\end{tabularx}
\end{tcolorbox}

\begin{tcolorbox}[breakable,title=Sincronización,colback=gray!4,colframe=black!55,fonttitle=\bfseries]
\footnotesize
\begin{tabularx}{\linewidth}{@{}p{1.25cm}p{5.35cm}p{1.9cm}X@{}}
\omniendpointheader
\texttt{POST} & \path{/api/sync/batch} & Usuario & Sube el lote de registros pendientes \\
\texttt{GET} & \path{/api/sync/changes} & Usuario & Descarga cambios desde la última sincronización \\
\end{tabularx}
\end{tcolorbox}

La exportación científica no dispone de un \textit{endpoint} propio: el portal web
genera los ficheros CSV (observaciones, ítems, resumen y un formato científico con
diccionario de columnas) y GeoJSON a partir de los datos ya consultados. El contrato
completo de la API se documenta con OpenAPI y puede explorarse de forma interactiva en
\texttt{/api/docs} mediante Swagger UI.

\section{Seguridad y control de acceso}

El sistema se diseña con autenticación basada en JWT y dos \textit{tokens}: un
\textbf{token de acceso} de corta duración (15 minutos) y un \textbf{token de refresco}
de larga duración (7 días). El token de acceso se envía en cada petición en el encabezado
\texttt{Authorization}; el de refresco se utiliza exclusivamente para solicitar un nuevo
token de acceso cuando el anterior expira.

La autorización sigue un modelo de control de acceso basado en roles (RBAC)
a nivel de grupo. El \textit{middleware} de autorización verifica, para cada petición,
que el usuario autenticado posee el rol requerido en el grupo al que pertenece el
recurso solicitado. Esto permite que una misma persona sea administradora en un grupo
y colaboradora en otro, con permisos diferentes en cada contexto.

% ------------------------------------------------------------
\section{Diseño del portal web}
% ------------------------------------------------------------

\subsection{Arquitectura de componentes}

El portal web se diseña como una \textit{Single Page Application} (SPA) con
React 18 y TypeScript, empaquetada con Vite y estilizada con Tailwind CSS. El
enrutado se resuelve en el cliente con la API \texttt{History} del navegador
(\texttt{pushState} y \texttt{replaceState}), sin una librería de enrutado externa, y
distingue dos niveles de rutas:

\begin{itemize}
  \item \textbf{Rutas públicas:} la página de inicio (\texttt{/}) y el formulario
    de inicio de sesión (\texttt{/login}), accesibles sin autenticación.
  \item \textbf{Rutas protegidas:} las páginas privadas bajo \texttt{/app/*}, que
    comprueban el estado de autenticación antes de renderizarse y redirigen al
    \textit{login} si no existe una sesión activa.
\end{itemize}

La estructura de componentes sigue una jerarquía de tres niveles: el \texttt{Layout}
general (barra lateral, encabezado, selector de grupo activo) envuelve las páginas
individuales, que a su vez utilizan componentes reutilizables (tarjetas de estadística,
tablas filtrables, modales de confirmación). Las vistas geográficas se construyen con
Leaflet a través de \texttt{react-leaflet}, con agrupación de marcadores y una capa de
mapa de calor, y los gráficos del dashboard y de la analítica se generan con Recharts.

\subsection{Gestión de estado}

El portal no utiliza una librería de gestión de estado externa. El estado de la
aplicación se mantiene en componentes React y se transmite mediante propiedades, algo
suficiente para la escala del portal. La sesión del usuario se conserva entre recargas
en el almacenamiento del navegador, donde se guardan los \textit{tokens} de acceso y
refresco emitidos por el backend, y se renueva de forma automática cuando el token de
acceso expira.

El grupo activo forma parte de ese estado de sesión. Al cambiarlo, las vistas que
dependen del grupo (campañas, observaciones, mapa, dashboard y analítica) recargan su
contenido para reflejar únicamente los datos del grupo seleccionado.

\subsection{Flujo de navegación y control de acceso por rol}

Las secciones de gestión de grupos y usuarios están reservadas al rol de
administrador. El control efectivo de acceso lo aplica el backend, que rechaza
cualquier operación no autorizada aunque la vista llegue a cargarse; la interfaz, por
su parte, oculta o deshabilita las acciones de administración para los colaboradores.
El Dashboard ajusta el contenido según el rol: los administradores ven las estadísticas
del grupo activo, mientras que los colaboradores ven sus propias métricas. La tabla de
observaciones muestra a cada colaborador sus registros y al administrador los de todo
el grupo.

% ------------------------------------------------------------
\section{Diseño de la aplicación móvil}
% ------------------------------------------------------------

\subsection{Stack tecnológico y estructura de pantallas}

La aplicación móvil está planteada con \textbf{React Native} y \textbf{Expo}, usando
\textit{development builds} o \textit{prebuild} cuando sea necesario acceder a
capacidades nativas del dispositivo. Esta aproximación permite mantener las ventajas
del ecosistema Expo sin renunciar a módulos como \texttt{expo-location},
\texttt{expo-camera} y las tareas de sincronización en segundo plano mediante
\texttt{expo-task-manager} y \texttt{expo-background-task}.

La navegación está organizada en dos niveles:

\begin{itemize}
  \item \textbf{Navegación de primer nivel (tab bar):} cinco pestañas inferiores
    (Inicio, Sesiones, Mapa, Sincronización, Perfil), visibles únicamente en las
    pantallas principales.
  \item \textbf{Navegación de pila (\textit{stack}):} flujos de varios pasos como la
    creación de una nueva sesión o la captura de una observación, que se presentan
    sin el \textit{tab bar}.
\end{itemize}

\subsection{Gestión de estado global}

El estado global de la aplicación se gestiona mediante el patrón
\textbf{Context + useReducer} de React, sin dependencias externas de gestión de
estado. El contexto \texttt{AppContext} expone el estado completo (sesiones activas,
tipos de residuo, datos del usuario, estado de sincronización) y un \textit{dispatcher}
con acciones tipadas en TypeScript. Este patrón proporciona la predictibilidad de
Redux con una superficie de código significativamente menor, adecuada para la escala
del proyecto.

\subsection{Base de datos local con SQLite}

SQLite, integrado en Expo mediante \texttt{expo-sqlite}, actúa como base de datos
local de la aplicación móvil. Se elige por ser una solución madura, ligera y
suficientemente potente para almacenar el volumen de datos esperado durante una
jornada de muestreo. Además, permite mantener control directo sobre el esquema local
y sobre la cola de sincronización, reduciendo la complejidad respecto a incorporar
una capa adicional de abstracción.

El esquema local replica el subconjunto del modelo de datos necesario para la
operación en campo: \texttt{sampling\_sessions}, \texttt{observations},
\texttt{litter\_items} y \texttt{taxonomy\_items}, junto con las tablas de apoyo de
usuarios, grupos, campañas y catálogo (\texttt{environments} y \texttt{typologies})
que se descargan al iniciar sesión. Las operaciones pendientes de subida se registran
en una tabla \textit{outbox} (\texttt{sync\_outbox}) que guarda, por cada cambio, el
\textit{endpoint} y el \textit{payload} de la petición, el número de intentos, el
último error y el estado de sincronización.

\subsection{Flujo de captura de una observación}

El flujo principal de la aplicación sigue esta secuencia:

\begin{enumerate}
  \item El investigador inicia una nueva sesión de muestreo, especificando el nombre,
    el tipo de entorno y los parámetros del transecto.
  \item Para cada ítem encontrado, abre la pantalla de nueva observación, selecciona
    el tipo de residuo a través del modal de taxonomía (organizado en cuadrícula visual
    por categorías e iconos), e introduce la cantidad, el tamaño, el color y
    comentarios.
  \item Opcionalmente captura una foto geolocalizada del residuo.
  \item Al finalizar la sesión de campo, revisa el resumen y puede iniciar la
    sincronización manualmente o dejar que se realice en segundo plano al recuperar
    cobertura de red.
\end{enumerate}

El tamaño y el color se recogen en el formulario como ayuda a la caracterización del
residuo, pero no se modelan como columnas propias. El ítem de residuo
(\texttt{litter\_items}) almacena la categoría, la cantidad y el material, y estas dos
anotaciones se serializan dentro de su campo de notas, de donde la aplicación las
vuelve a leer al editar la observación.

% ------------------------------------------------------------
\section{Sincronización offline}
% ------------------------------------------------------------

\subsection{Patrón Outbox Queue}

El mecanismo de sincronización se diseña siguiendo el patrón \textbf{Outbox Queue}
(cola de salida), ampliamente utilizado en sistemas distribuidos con conectividad
intermitente. El funcionamiento es el siguiente:

\begin{enumerate}
  \item Cuando el usuario crea o modifica un registro (sesión u observación) en el
    dispositivo sin conexión, el registro se persiste en SQLite con estado
    \texttt{PENDING} y se inserta una entrada en la tabla \texttt{sync\_outbox} con el
    \textit{payload} completo del cambio.
  \item Cuando el dispositivo recupera conectividad, una tarea de sincronización
    en segundo plano o una acción manual del usuario lee la cola y realiza una petición
    \texttt{POST /api/sync/batch} con todos los registros pendientes.
  \item El servidor procesa los registros en orden, los persiste en PostgreSQL y
    devuelve el estado canónico actualizado.
  \item El cliente actualiza el estado de los registros procesados a
    \texttt{SYNCED} y descarga los cambios remotos realizados por otros usuarios
    mediante el \textit{endpoint} \texttt{GET /api/sync/changes}, enviando la marca de
    tiempo de la última sincronización como parámetro de consulta.
  \item Si un registro concreto falla, pasa al estado \texttt{FAILED} cuando los datos
    son inválidos o la subida no se completa, o a \texttt{CONFLICT} cuando el servidor
    detecta una versión más reciente. En ambos casos se notifica al usuario sin
    bloquear la sincronización del resto.
\end{enumerate}

\subsection{Estados de sincronización}

Cada sesión y observación puede encontrarse en uno de cuatro estados, como muestra
la tabla~\ref{tab:sync_states}.

\begin{table}[H]
  \centering
  \caption{Estados de sincronización y su significado.}
  \label{tab:sync_states}
  \begin{tabularx}{\textwidth}{lX}
    \toprule
    \textbf{Estado} & \textbf{Significado} \\
    \midrule
    \texttt{PENDING}  & El registro existe solo en el dispositivo y está pendiente de
                        subir al servidor. \\
    \texttt{SYNCED}   & El servidor ha confirmado la recepción y persistencia del
                        registro. \\
    \texttt{FAILED}   & La subida no se completó o el servidor rechazó el registro por
                        datos inválidos. Requiere reintento. \\
    \texttt{CONFLICT} & El servidor detectó una versión más reciente del registro.
                        Requiere resolución. \\
    \bottomrule
  \end{tabularx}
\end{table}

\subsection{Resolución de conflictos}

La estrategia de resolución de conflictos propuesta es \textbf{last-write-wins} con
granularidad de registro: si dos usuarios modifican la misma sesión de forma
independiente mientras están sin conexión, prevalece la versión con el
\texttt{timestamp} más reciente. Esta estrategia es suficiente para el modelo de uso
de OmniLitter, donde cada sesión de muestreo pertenece a un único observador
responsable y los conflictos genuinos son infrecuentes.

\subsection{Garantía de identificadores únicos en entorno offline}

Para que la estrategia Outbox Queue funcione sin depender de un identificador
asignado por el servidor en el momento de la captura, los registros creados en modo
offline usan \textbf{UUID v4 generados en el cliente}. Esto garantiza que las
sesiones y observaciones creadas simultáneamente en distintos dispositivos no
colisionarán al sincronizarse, independientemente del orden de llegada al servidor.
